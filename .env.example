# KèoCầuPro Environment Configuration
# Copy this file to .env and fill in your PostgreSQL credentials

# PostgreSQL Database (optional - falls back to in-memory mock if not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=badminton_hub
DB_USER=postgres
DB_PASSWORD=your_password_here

# API Server Port
PORT=3001
